# DevBuddy config file
# Run `bud --doc` to learn how to use dev.yml and bud.

env:
  ENV: development

up:
  # MacOS:
  # - homebrew:
  #   - curl
  #   - golangci/tap/golangci-lint

  # Linux:
  # - apt:
  #   - python3-dev

  - go:
      version: 1.25.1

  # - custom:
  #     name: Create the local tmp directory
  #     met?: test -d tmp
  #     meet: mkdir tmp

commands:
  test:
    desc: Run tests
    run: go test -cover ./...

open:
  devbuddy: https://github.com/devbuddy/devbuddy/blob/main/Documentation.md
